import styled from 'styled-components';

const MapStyles = styled.div`
  height: 250px;
  max-height: 300px;

  .map-wrap {
    height: 100%;
  }

  #map {
    box-shadow: var(--bs);
    height: 100%;
  }
`;

export { MapStyles };
