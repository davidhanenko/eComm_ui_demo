import styled from 'styled-components';

const ImageSlideStyles = styled.div`
  background: transparent;
  margin: 0 1rem 1rem 1rem;
  position: relative;
  padding: 1rem;
  overflow: none;
`;

export { ImageSlideStyles };
